// Shared widgets for splash screen feature
// This file can be used for reusable widgets specific to splash screen

import 'package:flutter/material.dart';

/// Placeholder for splash screen specific widgets
class SplashWidget extends StatelessWidget {
  const SplashWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
